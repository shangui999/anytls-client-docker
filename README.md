# anytls-client Docker Image

这是一个基于 GitHub Actions 自动构建的 `anytls-client` 镜像。它轻量、安全，并且原生支持多架构（如普通的云服务器、Mac M 系列芯片或树莓派）。

---

## 🚀 快速开始

使用以下命令运行容器：

```bash
docker run -d \
  --name anytls-client \
  --restart always \
  -p 10808:10808 \
  -e PASS="你的密码" \
  -e HOST="你的服务器地址" \
  -e PORT="8443" \
  ghcr.io/你的用户名/你的仓库名:main

```

---

## ⚙️ 配置参数 (Environment Variables)

可以通过 `-e` 参数传递以下环境变量来配置客户端：

| 变量名 | 默认值 | 说明 |
| --- | --- | --- |
| `PASS` | `your_password` | anytls 服务端的连接密码 |
| `HOST` | `your_host` | anytls 服务端的域名或 IP |
| `PORT` | `8443` | anytls 服务端的端口 |
| `EXPOSE` | `10808` | 容器内部监听端口（Socks5 代理端口） |

---

## 🛠️ 构建细节

### 自动化流程

本项目使用 GitHub Actions 进行持续集成：

* **自动抓取版本**：构建脚本会自动从 [anytls-go](https://github.com/anytls/anytls-go) 官方仓库获取最新的 Release 版本。
* **多架构支持**：利用 Docker Buildx 同时构建并推送以下架构的镜像：
* `linux/amd64` (标准服务器)
* `linux/arm64` (Apple Silicon, Raspberry Pi, ARM 云服务器)



### 基础镜像

* 基于 `alpine:3.21`，镜像总体积保持在 **10MB** 左右。

---

## 📖 本地手动构建

如果你想在本地自行修改并测试：

```bash
# 启用 Buildx
docker buildx create --use

# 构建多架构镜像
docker buildx build \
  --platform linux/amd64,linux/arm64 \
  --build-arg VERSION=0.0.12 \
  -t anytls-client:local .

```

---

## 📄 开源协议

本项目仅提供 Docker 打包脚本。`anytls-go` 二进制文件的版权归其原作者所有。
